<Window x:Class="PizzaStore.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PizzaStore.WPF"
        xmlns:c="clr-namespace:PizzaStore.WPF.Controls"
        mc:Ignorable="d"
        Title="Pizza Store" 
        Height="675" Width="1200"
        MinHeight="675" MinWidth="1200">
    <Window.Background>
        <ImageBrush Opacity="0.5" ImageSource="Resources/pizza.jpg" />
    </Window.Background>
    
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <c:NavigationBar Grid.Row="0" DataContext="{Binding Navigator}" />

        <ScrollViewer Grid.Row="1" Padding="20 0" VerticalScrollBarVisibility="Auto">
            <ContentControl Margin="10" Content="{Binding Navigator.CurrentViewModel}" />
        </ScrollViewer>

    </Grid>
</Window>